// You can pass any object to the arguments parameter. In this example,
// create a class that contains both a customizable title and message.
class PassArgsModel {
  final String title;
  final String message;

  PassArgsModel(this.title, this.message);
}